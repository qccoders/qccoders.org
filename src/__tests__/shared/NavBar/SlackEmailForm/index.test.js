/* 
Things to test:
1. That the navbar shows up
2. That the list of routes = the routes that we've set up
3. That the slack email form exists
    a. That when a user inputs their email in the address, the state updates
    b. That when a the form is submitted, sendSlackInvite() is called
    c. That when a user enters their email in the address, npm.validator says it's a valid email
    d. That when an invalid email gets entered, an error message is thrown
    e. That when a valid email is entered a success message is shown

*/
test('stubbed test', () => {
    expect(true).toBe(true)
})